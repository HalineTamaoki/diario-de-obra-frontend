import React, { useCallback } from 'react'

export const ExecucaoCard = ({id}: {id: number}) => {
    const openCalendar = useCallback((idData: number) => {

    }, []);

    return (
        <div>

        </div>
    )
}
